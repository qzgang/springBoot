debug: false
server:
  port: 8888
  servlet.context-path: /sso

spring:
  cloud:
    zookeeper:
      connect-string: @zkServer@
      config:
        failFast: false
        root: /hurbao
        defaultContext: conf
        enabled: true
        profileSeparator: ','
  application:
    name: /conf/hrb-test
